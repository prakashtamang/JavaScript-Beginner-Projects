## Counter App

A simple JavaScript counter application that demonstrates how JavaScript interacts with HTML and CSS to update the user interface dynamically.

### Features

- Display the current counter value.

- Increment the counter by one.

- Decrement the counter (does not go below 0).

- Reset the counter back to 0.
